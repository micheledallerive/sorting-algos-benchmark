package tests;

/**
 * Generic interface for an experiment.
 */
public interface Experiment {
    /**
     * Run the experiment.
     *
     * The output of the experiment is printed to stdout.
     */
    void run();
}
